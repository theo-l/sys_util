#!/bin/bash

alias vagrant_init='vagrant init'
alias vagrant_up='vagrant up'
alias vagrant_halt="vagrant halt"
alias vagrant_reload="vagrant reload"
alias vagrant_ssh="vagrant ssh"
alias vagrant_status="vagrant status"
alias vagrant_destroy="vagrant destroy"
